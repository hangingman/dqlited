version: "3"

vars:
  RELEASE: jammy
  GITHUB_USER: hangingman
  IMG: "{{.GITHUB_USER}}/dqlited:{{.RELEASE}}"

tasks:
  hey:
    desc: just echo target ubuntu version
    cmds:
      - "echo RELEASE: {{.RELEASE}}"
